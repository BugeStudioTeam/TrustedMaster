<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TrustedMaster.Views.PermissionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="40" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Manage Authorized Applications" FontSize="28" FontWeight="SemiBold"/>

        <StackPanel Grid.Row="1" Spacing="10">
            <TextBlock Text="Applications that have been granted elevated privileges." TextWrapping="Wrap" FontSize="16" Opacity="0.7"/>

            <ListView x:Name="AppsListView" Height="300">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="20">
                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Status}" VerticalAlignment="Center" FontStyle="Italic" Opacity="0.6"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>