<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TrustedMaster.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="40" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Home" FontSize="28" FontWeight="SemiBold"/>

        <TextBlock Grid.Row="1" Text="Select the privilege level to execute commands or launch applications." TextWrapping="Wrap" FontSize="16" Opacity="0.7"/>

        <StackPanel Grid.Row="2" Spacing="15">
            <Button x:Name="AdminButton" Content="Run as Administrator" Click="OnAdminButtonClick" Style="{StaticResource AccentButtonStyle}"/>
            <Button x:Name="SystemButton" Content="Run as SYSTEM" Click="OnSystemButtonClick"/>
            <Button x:Name="TrustedInstallerButton" Content="Run as TrustedInstaller" Click="OnTrustedInstallerButtonClick"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Text="Grant all privileges by default (Example)" FontSize="14" Opacity="0.5"/>

        <StackPanel Grid.Row="4" VerticalAlignment="Bottom">
            <TextBlock x:Name="StatusText" Text="Ready" FontSize="14"/>
        </StackPanel>
    </Grid>
</Page>